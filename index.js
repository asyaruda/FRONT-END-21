const { addCount,
        subCount,
        multCount,
        divCount } = require('./calc-node/index.js')

       console.log(addCount(0, 2))
       console.log(subCount(12, 5))
       console.log(multCount(7, 4))
       console.log(divCount(13, 2))